const hamburger=document.getElementById("hamburger"),mobileMenu=document.getElementById("mobile-menu"),closeBtn=document.getElementById("close-btn");function toggleMenu(){hamburger.classList.toggle("active"),mobileMenu.classList.toggle("active")}hamburger.addEventListener("click",toggleMenu),closeBtn.addEventListener("click",toggleMenu);const mobileLinks=mobileMenu.querySelectorAll("a");mobileLinks.forEach(e=>{e.addEventListener("click",toggleMenu)});